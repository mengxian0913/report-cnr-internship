= 國外實習企業或機構之學習心得

這次在 CNR-ISP 的實習，是第一次真正進入研究機構的工作環境。與學校的課程或專題不同，這裡面對的是實際運作中的系統與真實的需求，以下分幾個面向記錄這段期間的學習與觀察。

== 技術層面

實習期間全程參與 ParticularMatter 專案的開發，工作橫跨嵌入式韌體與 Web 前端兩個層面，並負責對應的技術文件撰寫。以下依三個面向說明。

=== 韌體開發（ESP32 / C++ / PlatformIO）

首要工作是將既有韌體從舊架構遷移至 ESP32 + PlatformIO 的專案結構，並在此基礎上逐步開發各功能模組：

- *感測器驅動*：從零實作 PMS5003（PM1.0/2.5/10 懸浮微粒）、BME280（溫濕度／氣壓）、NEO-6M GPS（NMEA 解析）、SenseAir（CO2）等感測器的驅動程式，涉及 UART 與 I2C 通訊協定，並設計 `GenericSensorBase` 抽象類別統一各感測器介面
- *WiFi 管理與設定入口*：實作 AP/STA 雙模 WiFi 管理，包含 Captive Portal（強制導向設定頁）、WiFi 掃描、憑證儲存與重連邏輯，以及設備設定的 NVS 持久化儲存
- *資料記錄與時間同步*：實作 SD 卡每日 CSV 日誌寫入，並整合 NTP 時間同步，確保每筆量測資料帶有正確的 UTC 時間戳記
- *MQTT 發布模組*：設計並實作 MQTT 訊息發布機制，採用 per-sensor topic 架構，讓各感測器的資料能獨立訂閱
- *REST API 模組化*：將 Web Portal 的 API handler 依功能拆分為 System、WiFi、Node、Measurement 四個模組，提供設備狀態查詢、WiFi 設定、資料下載等共 16 支端點

=== 前端開發（Vue 3 / TypeScript / Vite）

前端專案從零建立，最終編譯後整合進韌體的 LittleFS，由 ESP32 上的 Web Server 直接服務：

- 建立完整的前端工程基礎：Vite、ESLint、Vue Router、Pinia 狀態管理、i18n 多語系
- 設計 API service 層並搭配 MSW（Mock Service Worker）實作 mock handler，使前端開發不依賴實體裝置
- 實作初始化精靈（Init Stepper）：以 state machine 管理使用者完成 WiFi 連線與裝置配對的分步驟引導流程
- 開發各功能頁面：系統儀表板、WiFi 設定、量測資料列表（含篩選與 CSV 下載）、系統 log 檢視器、即時感測器數值顯示（含品質等級標示）

=== 技術文件

實習期間同步撰寫各模組的技術文件，涵蓋所有 API endpoint 的 request/response 規格、MQTT topic 定義、韌體模組說明、型別定義，以及六種感測器品質等級（enum）的說明文件，共計 16 份以上。

== 跨文化協作

實驗室的成員來自不同國家與學術背景，日常溝通以英文進行。這樣的環境讓人快速習慣用非母語表達技術問題，也學到如何在討論中確認彼此的理解是否一致，而不只是點頭帶過。

義大利職場的步調與台灣有些不同，會議和決策的過程相對開放，成員之間較常直接提出意見。這種工作方式一開始需要適應，但也讓人更清楚地看到自己在團隊中能貢獻什麼。

== 研究環境的觀察

CNR-ISP 是學術研究機構，與一般企業的目標導向不同，這裡更重視問題的嚴謹性與資料的可信度。實習期間觀察到研究人員如何設計實驗、處理異常資料，以及如何在不確定性中做出判斷，這些都是在課堂上較難直接接觸到的工作現實。